"""
       Функция выводит строку, которой присвоена переменная message.
        """


def text():
    """
    Присваевает значение строки переменной message.

    Parameters
    ----------
    message : str
        Переменная, которой присваивается значение

    Returns
    -------
    str
        Значение переменной message
    """
    message = "message"
    return message
